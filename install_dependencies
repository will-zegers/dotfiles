#! /usr/bin/env bash

set -ex

apt -y update
apt -y install \
  curl \
  ddate \
  exuberant-ctags \
  git \
  screenfetch \
  tmux \
  vim \
  zsh \
;

# only install terminator on host machines
if systemd-detect-virt
then
    apt -y install terminator
fi
apt -y autoremove
